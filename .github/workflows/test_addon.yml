name: Kodi Addon Check & Lint

on: [push, pull_request]

jobs:
  kodi-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install flake8 kodi-addon-checker

      - name: Run Python Lint (flake8)
        run: |
          flake8 . --count --select=E9,F63,F7,F82 --ignore=W503 --show-source --statistics
          flake8 . --count --exit-zero --ignore=W503 --max-line-length=127 --statistics

      - name: Prepare Workspace for Kodi Checker
        run: |
          rm -f .gitattributes .gitignore
          rm -rf zips/

      - name: Run Kodi Addon Checker
        run: kodi-addon-checker --branch omega --allow-folder-id-mismatch .
